#!/bin/sh
set -e
while [[ ! -d /sys/class/net/ovpn-surfshark ]]; do sleep 0.1; done
while [[ ! -f /tmp/ovpn-surfshark-trusted-ip ]]; do sleep 0.1; done
while ! nc -z $(cat /tmp/ovpn-surfshark-trusted-ip) 1443; do sleep 0.1; done
